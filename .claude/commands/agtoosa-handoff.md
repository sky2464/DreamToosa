Read @Docs/AgToosa_Handoff.md and execute the handoff pack export workflow.

Arguments provided: $ARGUMENTS

Use $ARGUMENTS to resolve the target sub-command (`wave` or `task`) and any story ID or title fragment. If no argument is provided, run the full flow (Step 1: Resolve target) and ask the user interactively.

On successful completion, print this line verbatim: `Next: /agtoosa-<command> — <rationale>` plus `SYNC:` pulse (see Lifecycle Next-Step Contract)

<!-- AGTOOSA PRODUCT TRUTH START: command.handoff -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-handoff` (`command.handoff`)
- Canonical workflow: `Docs/AgToosa_Handoff.md`
- Modes: `full`, `wave`, `task`
- Question budget: max 0; Quick max: 0
- Mutation class: `write`
- Approval gate: none
- Phase stop: none
- Lifecycle close: `none`
<!-- AGTOOSA PRODUCT TRUTH END: command.handoff -->
